name: Build and Publish Wheels

on:
  workflow_dispatch:
  push:
    branches: ["main", "mcp2"]
  pull_request:
    branches: ["main", "mcp2"]

jobs:
  build:
    uses: ./.github/workflows/reusable-build-wheel.yml
    with:
      python-version: "3.11"
